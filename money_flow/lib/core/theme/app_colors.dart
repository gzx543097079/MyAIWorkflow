import 'package:flutter/material.dart';

class AppColors {
  const AppColors._();

  static const primarySeed = Color(0xFF2563EB);
  static const pageBackground = Color(0xFFF8FAFC);
  static const darkPageBackground = Color(0xFF0F172A);
}
